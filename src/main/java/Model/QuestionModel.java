package Model;

import Controller.ExamController.Exam;
import Controller.QuestionController.Question;

import java.sql.SQLException;

public class QuestionModel {
/*
    public static Question question;

    public static void addTrueFalseQuestion(Question question) {

        String Query = "INSERT into Question(question_id, answer1, answer2) VALUES (" +"'" + question.getClass().get+"'" + ", "+"'" + exam.getName() +"'" +" ," +"'" + exam.getGrade()+"'" + ") ;";
        try {
            DBConnection.connection.createStatement().execute(Query);

        } catch (SQLException e) {

            e.printStackTrace();
        }

    }

    public static void addTextQuestion(Question question) {

        String Query = "INSERT into Question(exam_id, exam_name, exam_grade) VALUES (" +"'" + exam.getId()+"'" + ", "+"'" + exam.getName() +"'" +" ," +"'" + exam.getGrade()+"'" + ") ;";
        try {
            DBConnection.connection.createStatement().execute(Query);

        } catch (SQLException e) {

            e.printStackTrace();
        }

    }

    public static void addMultipleChoiceQuestion(Question question) {

        String Query = "INSERT into Question(exam_id, exam_name, exam_grade) VALUES (" +"'" + exam.getId()+"'" + ", "+"'" + exam.getName() +"'" +" ," +"'" + exam.getGrade()+"'" + ") ;";
        try {
            DBConnection.connection.createStatement().execute(Query);

        } catch (SQLException e) {

            e.printStackTrace();
        }

    } */
}
